
#include <iostream>
#include <map>

//#include "../GameDeck.hpp"


//In terms of setting up the game pieces, they get setup when
//the map gets deserialized.
int main(int argc, char** argv)
{

    /*GameDeck provider;

    std::cout << "There are " << provider.getCoinsTokenListSize() << " coins in the deck." << std::endl;
    std::cout << "There are " << provider.getMapTokensTokenListSize() << " map tokens in the deck." << std::endl;
    std::cout << "There are " << provider.getRaceBannersTokenListSize() << " race banners in the deck." << std::endl;
    std::cout << "There are " << provider.getRaceTokensTokenListSize() << " race tokens in the deck." << std::endl;
    std::cout << "There are " << provider.getSpecialPowersTokenListSize() << " badges/special powers in the deck." << std::endl;

    std::cout << "Here is a list of the coins in the deck." << std::endl;
    for (auto elem : *provider.getCoinsTokenList())
    {
        std::cout << getTokenString(elem) << std::endl;
    }
    std::cout << std::endl;

    std::cout << "Here is a list of map tokens in the deck." << std::endl;
    for (auto elem : *provider.getMapTokensTokenList())
    {
        std::cout << getTokenString(elem) << std::endl;
    }
    std::cout << std::endl;

    std::cout << "Here is a list of the race tokens in the deck." << std::endl;
    for (auto elem : *provider.getRaceBannersTokenList())
    {
        std::cout << getTokenString(elem) << std::endl;
    }
    std::cout << std::endl;

    std::cout << "Here is a list of the race tokens in the deck." << std::endl;
    for (auto elem : *provider.getRaceTokensTokenList())
    {
        std::cout << getTokenString(elem) << std::endl;
    }
    std::cout << std::endl;

    std::cout << "Here is a list of the badges/special power in the deck." << std::endl;
    for (auto elem : *provider.getSpecialPowersTokenList())
    {
        std::cout << getTokenString(elem) << std::endl;
    }
    std::cout << std::endl;

    return 0;*/
}


